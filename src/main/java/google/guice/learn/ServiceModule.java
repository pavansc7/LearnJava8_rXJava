package google.guice.learn;

import com.google.inject.AbstractModule;

public class ServiceModule extends AbstractModule {

    public void configure(){
    //    bind(MessageService.class).to(PhoneService.class);
    }
}
